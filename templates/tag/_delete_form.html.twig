<form method="post" action="{{ path('app_tag_delete', {'id': tag.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer {{tag.name}} ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
	<button class="btn icone"><i class="fa-solid fa-trash"></i></button>
</form>

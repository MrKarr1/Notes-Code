{% extends 'base.html.twig' %}
{% set nav = true %}
{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1>{{ tag.name }}</h1>

    	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}


    <button class="btn" ><a href="{{ path('app_tag_add') }}">Retour</a></button>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}

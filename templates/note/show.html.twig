{% extends 'base.html.twig' %}
{% set nav = true %}
{% block title %}
	{{ note.name }}
{% endblock %}

{% block body %}
{% include "include/nav.html.twig" %}

	<main class="show_note">
		<h1>{{ note.name }}</h1>
		{% if note.tag %}
			{% for tag in note.tag %}
				<span>{{tag.name}}</span>
			{% endfor %}
		{% else %}
			<span>Pas de Tag</span>
		{% endif %}
		{% if note.folder %}
			{% for folders in note.folder %}
				<span>{{folders.name}}</span>
			{% endfor %}
		{% else %}
			<span>Pas de dossier</span>
		{% endif %}
		<span>{{ note.langage.displayName }}</span>
		<span>{{ note.createdAt ? note.createdAt|date('d-m-Y') : '' }}</span>
        <div id="note-code" style="display: none;">{{ note.code }}</div>
        <div id="note-language" style="display: none;">{{ note.langage.technicalName|lower }}</div>
		<div id="editor" style="width: 100%; height: 500px; border: 1px solid #ccc;"></div>
		<p>{{note.description}}</p>

		<button class="btn">
			<a href="{{ path('app_account') }}">retour</a>
		</button>
		<button class="btn">
			<a href="{{ path('app_note_edit', {'id': note.id}) }}">Modifier</a>
		</button>

		{{ include('note/_delete_form.html.twig') }}
	
	</main>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.52.2/min/vs/loader.min.js"></script>
    <script src="{{ asset('monaco/view.js') }}"></script>
{% endblock %}

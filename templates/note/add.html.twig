{% extends 'base.html.twig' %}

{% block body %}
	<div class="nav-laterale">
		{% include "include/nav.html.twig" %}
	</div>
	<div class="carte">
		<main class="edit-note">
			<div class="btn-EAS"></div>
			<div class="flex-button">
				<button class="btn">
					<a href="{{ path('app_account') }}">Retour</a>
				</button>
			</div>
			<div class="EAS-code-des">
				<h2>Ajouter Une Note</h2>
				{{ form_start(form) }}
				{{ form_row(form.name) }}
				{{ form_row(form.code, {'attr': {'class': 'monaco-editor'}}) }}
				{{ form_row(form.description) }}
				<div class="flex">
					<div class="right-note">
						{{ form_row(form.img) }}
						{{ form_row(form.is_favori) }}
						{{ form_row(form.langage) }}
					</div>
					<div class="left-note">
						{{ form_row(form.tag) }}
						{{ form_row(form.folder) }}
					</div>
				</div>
				<div class="flex-button">
					<button class="btn">Valider</button>
				</div>
				{{ form_end(form) }}
			</div>
		</main>
	</div>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.52.2/min/vs/loader.min.js"></script>
	<script src="{{ asset('monaco/add-edit.js') }}"></script>
{% endblock %}
